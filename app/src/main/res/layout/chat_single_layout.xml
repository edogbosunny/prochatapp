<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/chat_single_relative_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginLeft="5dp"
    android:layout_marginRight="5dp"
    android:orientation="horizontal">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/chat_single_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:src="@drawable/user2"
        android:layout_marginTop="12dp"
        android:layout_marginLeft="12dp"
        android:layout_marginRight="12dp"
        android:layout_marginBottom="12dp" />

    <TextView
        android:id="@+id/chat_single_last_msg"
        android:text="User's Status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:layout_alignBottom="@+id/chat_single_image"
        android:layout_toEndOf="@+id/chat_single_image"
        android:layout_marginBottom="10dp" />

    <ImageView
        android:id="@+id/chat_single_online"
        android:layout_width="10dp"
        android:layout_height="10dp"
        app:srcCompat="@mipmap/ic_online"
        android:layout_marginRight="2dp"
        android:visibility="invisible"
        android:layout_alignBottom="@+id/chat_single_image"
        android:layout_alignEnd="@+id/chat_single_image" />

    <TextView
        android:id="@+id/chat_single_username"
        android:text="Username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:textSize="18sp"
        android:layout_above="@+id/chat_single_last_msg"
        android:layout_toEndOf="@+id/chat_single_image" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/chat_single_image"
        android:background="#bdbdbd" />


</RelativeLayout>